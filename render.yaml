services:
  - type: web
    name: propcall-voice-engine
    env: node
    buildCommand: npm install
    startCommand: npm run server:prod
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: PORT
        value: 4000
